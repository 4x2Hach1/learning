extend type Query {
  user(id: Int!): User
}

extend type Mutation {
  newUser(input: NewUser!): Boolean!
}

type User {
  id: Int!
  name: String!
  email: String!
}

input NewUser {
  name: String!
  email: String!
}
