syntax = "proto3";
package server;
option go_package = "./pb";

import "proto/hello.proto";
import "proto/user.proto";

service ServerService {
    rpc Hello(hello.HelloRequest) returns(hello.HelloResponse);
    rpc User(user.UserRequest) returns(user.UserResponse);
}

